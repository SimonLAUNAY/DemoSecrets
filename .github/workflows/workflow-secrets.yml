name: Test Secrets and variables
on: push
jobs: 
  display_values:
    runs-on: ubuntu-latest
    steps:
      - run: |
         echo "${{ secrets.DATABASE_PASSWORD }}"
         echo "${{ vars.VERSION }}"
